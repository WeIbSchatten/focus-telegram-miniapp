# Токен бота от @BotFather (тот же, что и в focus-service для валидации initData)
TELEGRAM_BOT_TOKEN=8375618909:AAG8eL4p_NimneWw8vNcNHZGbCpWTMGMtUc

# Публичный URL Mini App (HTTPS). Домен, на котором развёрнут фронт (например Timeweb VPS).
# Для деплоя на focusvn.mooo.com укажите: https://focusvn.mooo.com
MINI_APP_URL=https://focusvn.mooo.com

# Секрет для приёма POST /notify от Focus Kids (задай свой длинный случайный ключ)
NOTIFY_SECRET=change_me_notify_secret

# URL Focus service (для запроса telegram_user_id по focus_user_id). В Docker: http://focus-service:3000
FOCUS_SERVICE_URL=http://focus-service:3000

# URL Focus Kids API (для команды /status — новое ДЗ и тесты). В Docker: http://focus-kids-service:8000
KIDS_API_URL=http://focus-kids-service:8000

# Секрет внутреннего API Focus service и Kids (тот же INTERNAL_API_SECRET, что в focus-service и focus-kids-service)
INTERNAL_API_SECRET=change_me_internal_secret

# Порт HTTP-сервера для приёма уведомлений (POST /notify)
NOTIFY_PORT=4000
